spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/university
    username: root
    password: Abhishek@1009
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate.ddl-auto: update
    show-sql: true